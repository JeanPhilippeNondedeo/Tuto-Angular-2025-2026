import { ExponentialStrenghPipe } from './exponential-strengh-pipe';

describe('ExponentialStrenghPipe', () => {
  it('create an instance', () => {
    const pipe = new ExponentialStrenghPipe();
    expect(pipe).toBeTruthy();
  });
});
